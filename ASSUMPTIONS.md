# Assumptions & Open Questions

- GA4 properties used during evaluation may contain low or zero traffic.
- Evaluators will replace `credentials.json` and set LLM environment variables at runtime.
- SEO audit data schema may vary; the system adapts dynamically to available columns.
- The evaluation focuses on correctness and robustness rather than data volume.
